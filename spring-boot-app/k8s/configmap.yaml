apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-world-config
  namespace: default
  labels:
    app: hello-world
data:
  app.name: "Hello World Spring Boot App"
  app.version: "1.0.0"
  environment: "kubernetes"
